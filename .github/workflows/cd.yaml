name: cd
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.repository.fork == false
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.x
      - run: pip install git+https://${GH_TOKEN}@github.com/tpucci/mkdocs-material-insiders.git
      - run: mkdocs gh-deploy --force
env:
  # We use a Personnal access token because GH_TOKEN is only scoped to this repo.
  GH_TOKEN: ${{ secrets.GH_PAT }}
